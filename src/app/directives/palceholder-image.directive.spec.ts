import { PalceholderImageDirective } from './palceholder-image.directive';

describe('PalceholderImageDirective', () => {
  it('should create an instance', () => {
    const directive = new PalceholderImageDirective();
    expect(directive).toBeTruthy();
  });
});
